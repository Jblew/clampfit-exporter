apiVersion: v1
kind: Service
metadata:
  name: backend
  labels: { service: backend }
spec:
  selector: { service: backend }
  ports: [{ port: 80, targetPort: 80 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels: { service: backend }
spec:
  replicas: 1
  selector: { matchLabels: { service: backend } }
  template:
    metadata: { labels: { service: backend } }
    spec:
      # imagePullSecrets: [{ name: dockerconfigjson-github-com }] # If the image was private on ghcr.io (consult 1myears/totus or 1myears/extensa-deploy)
      restartPolicy: Always
      containers:
        - name: backend
          image: ghcr.io/jblew/clampfit-exporter-backend:1.2.0
          ports: [{ containerPort: 80 }]
          env:
            - name: PORT
              value: "80"
            - name: BASE_URL
              value: "https://clamper.jblew.pl/api"
            - name: CREDENTIALS_USER
              valueFrom:
                secretKeyRef:
                  name: user-credentials
                  key: user
            - name: CREDENTIALS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: user-credentials
                  key: password
